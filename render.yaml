services:
  - type: web
    name: diagtamai-api
    runtime: node
    plan: free
    rootDir: diagram-ai/server
    buildCommand: npm install
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
