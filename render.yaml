services:
  - type: web
    name: diagtamai-api
    runtime: node
    plan: free
    buildCommand: cd diagram-ai/server && npm install
    startCommand: cd diagram-ai/server && npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
