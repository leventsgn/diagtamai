services:
  - type: web
    name: diagtamai-api
    runtime: node
    plan: free
    rootDirectory: diagram-ai/server
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
